#include <SDL2/SDL.h>

void putPixel (SDL_Renderer* &renderer, int x, int y);
void drawCircle (SDL_Renderer* &renderer, int x0, int y0, int radius);
void fillCircle (SDL_Renderer* &renderer, int x0, int y0, int radius);